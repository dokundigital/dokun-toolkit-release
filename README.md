# Dokun Toolkit Release Repository

Public distribution channel for Dokun Toolkit plugin packages.

This repository contains:

- `update.json` manifest consumed by WordPress updater
- release assets (`.zip`) published through GitHub Releases

Source code remains in a separate private repository.
